version: "3.9"

services:

  streamlit:
    build: .
    container_name: travel_streamlit
    ports:
      - "8501:8501"
    restart: always

  api:
    build: .
    container_name: travel_api
    command: python api.py
    ports:
      - "5000:5000"
    restart: always